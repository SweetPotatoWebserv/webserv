http {
    server {
        listen 0.0.0.0:8080;
        location / {
            root docs/html/;
            index index.html;
        }

        location /img {
            root docs/img/;
            index icon.jpeg;
        }
    }
}
