http {
    server {
        listen 127.0.0.1:8080;
        location / {
            root docs/html/;
            index index.html;
        }

        location /img {
            root docs/img/;
            index icon.jpeg;
        }
    }
}
